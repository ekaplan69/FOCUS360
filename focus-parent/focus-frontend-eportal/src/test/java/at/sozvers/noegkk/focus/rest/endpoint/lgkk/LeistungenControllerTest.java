package at.sozvers.noegkk.focus.rest.endpoint.lgkk;
/*
import at.sozvers.noegkk.focus.CdiExtension;
import at.sozvers.noegkk.focus.rest.endpoint.leistungen.LeistungenController;
import at.sozvers.noegkk.focus.service.common.ValidationException;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;

import jakarta.inject.Inject;

import static org.junit.jupiter.api.Assertions.assertThrows;

@ExtendWith(CdiExtension.class)
public class LeistungenControllerTest {
    @Inject
    LeistungenController controller;

    @Test
    public void testValidVSNR(){
        String validVSNR = "4263060898";
        controller.validateFachschluessel(validVSNR);
    }

    @Test
    public void testInvalidVSNR(){
        String invalidVSNR = "9874651111";
        assertThrows(ValidationException.class, () -> controller.validateFachschluessel(invalidVSNR));
    }

    @Test
    public void testValidBTNR(){
        String validBTNR = "0226231100";
        controller.validateFachschluessel(validBTNR);
    }

    @Test
    public void testInvalidBTNR(){
        String invalidBTNR = "012345678";
        assertThrows(ValidationException.class, () -> controller.validateFachschluessel(invalidBTNR));
    }
}*/
